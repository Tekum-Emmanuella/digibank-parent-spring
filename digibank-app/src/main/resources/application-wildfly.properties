# WildFly deployment profile (activate with --spring.profiles.active=wildfly)
# Relies on the java:/jdbc/DigiBankDS datasource declared in WildFly's standalone.xml.
spring.datasource.jndi-name=java:/jdbc/DigiBankDS
